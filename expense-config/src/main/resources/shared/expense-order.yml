spring:
   jpa:
      show-sql: true
      hibernate:
         ddl-auto: none
   datasource:
      url: jdbc:mysql://localhost:3306/expense
      username: root
      password: root
security:
   oauth2:
      resource:
         user-info-uri: http://localhost:9000/users/current
         grant-type: client_credentials
         scope: ui
server:
   port: 8082
eureka:
   client:
      default-zone: localhost:8761/eureka
logging:
   level:
      org:
         springframework: DEBUG