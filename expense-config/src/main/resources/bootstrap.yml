spring:
   cloud:
      config:
         server:
            native:
               search-locations: classpath:/shared
   profiles:
      active: native
server:
   port: 7000
logging:
   level:
      org:
         springframework: DEBUG